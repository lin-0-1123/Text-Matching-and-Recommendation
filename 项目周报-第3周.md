# 项目周报-第3周

日期：2025-04-13

项目实践题目：文本的匹配与推荐

## 实践内容
### 第八周任务：
1. Markdown Preview Enhanced插件下载。✅
2. 新建math.md 文件，并仿写指定部分的数学公式并上传到Github仓库。✅
3. 学习 markdown 语法 and 数学公式入门。✅

### 论文阅读：
- 嵌入：嵌入作为一种更强大的单词表示形式，将单词表示为short dense vector，相对于长的稀疏向量，可以更好的泛化和避免过拟合，同时在捕捉同义词方面也有更好的表现
- Word2vec:作为 SGNS 算法的简称（因为其属于 Word2vec 软件包下），将分类器的权重作为词嵌入的结果。相较于神经网络，Word2vec 简化了任务（使其成为二进制分类器而不是单词预测），并且简化了体系结构（训练逻辑回归分类器）。
- skip-gram：该模型为每个单词学习两个单独的嵌入：目标嵌入和上下文嵌入，分别储存在两个不同的矩阵中，通常将两个矩阵中对应的向量加在一起来表示一个单词。
- 可视化嵌入：有助于理解、应用和改进词义模型。下面是一写常见的嵌入方法：
    - 将词汇表中所有单词的向量按其余弦与目标词的向量进行排序
    - 使用聚类算法来显示在嵌入空间中单词与其他单词的关系（用层次来表示）
    - 将高维的嵌入空间向量投影到一个二维空间中（如t-SNE）
    
个人感觉 6.8 关于 Word2vec 的讲解我感觉自己并没有完全的理解，还需要花时间再重复阅读一下。
### 下阶段任务：
1、完成第九周的任务。
2、阅读“Description-Based Text Similarity”这篇论文。

