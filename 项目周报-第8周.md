# 项目周报-第6周

日期：2025-05-18

项目实践题目：文本的匹配与推荐

## 实践内容
### 

### 论文阅读——Precise Zero-Shot Dense Retrieval without Relevance Labels：
- 4 实验：
    - 4.1 配置：
        - 使用InstructGPT和Contriever模型来实现HyDE，使用 OpenAI 的默认温度值 0.7 来进行生成。使用仅包含英语的 Contriever 模型进行英语检索任务，使用多语言 mContriever 进行非英语任务。我们在 Pyserini 工具包（Lin et al.，2021a）上进行了检索实验。
        - 数据集：TREC DL19和DL20；它们基于MS MARCO数据集。还使用了来自BEIR数据集的6个低资源数据集。对于非英语检索，考虑了来自Mr. Tydi数据集的斯瓦希里语、韩语、日语和孟加拉语。
        - 为每个数据集使用不同的指令。它们具有相似的结构，但有不同的量词来控制生成的假设文档的确切形式
        - 对比体系：
            - 无监督的Contriever 和 mContriever为主要基准，与 HyDE 检索器共享嵌入空间（区别是向量的构建方法），BM25 也加入作为对比对象
        - 文中还说使用了一些微调数据集和微调过后的模型，他们在微调过后拥有更强的检索能力
    - 4.2 网络搜索：
        - 论文中给出了使用上述提到的模型（Contritever、BM25、HyDE、DPR、ANCE、ContrieverFT）在TREC DL19和DL20数据集上的检索效果，由于我队表中的一些数据并不了解，所以在分析表中数据前先给出一些属性的解释：
            - map：平均精度值
            - ndcg@10：归一化折损累计增益，在 top-10 的取值，用于评估检索结果的排序质量
            - recall@1000：召回率在检索 1000个文档时的取值，衡量模型找到相关文档的能力：
            - 有/无相关性判断：模型在训练和测试时有/无使用人工标注的相关性标签（即没有明确的“相关”或“不相关”的标注），个人认为这个就是有无监督学习的差异。
            - 关于模型：BM25 和 Contriever 都是无监督的，其中 contriever 作为主要基准，BM25 是论文发表时的主要检索模型，HyDE 是论文中提出的模型，DPR 和 ANCE 是有监督的模型，ContrieverFT 是在 Contriever 模型上进行微调后的模型。
        - 数据分析：在无相关性的情况下，HyDE 无疑是表现最好的模型，而在有相关性的情况下，HyDE 虽然没有在各项数据中领先很多甚至部分落后，但是综合来看与微调模型仍具有竞争力。
- 5 分析：
    - 使用不同的生成模型：数据表明，更大的生成模型为无监督的 Contriever 带来了提升
    - 使用不同的编码器：使用微调的 ContrieverFT 会发现，更弱的生成模型会带来负面影响，但因为 ContrieverFT 是在 Contriever 模型上进行微调的，所以性能下降程序较小。
- 6 结论：这篇论文介绍了一种新的 LLM 与密集编码器/检索器之间交互的新范式。作者证明了（部分）相关性建模和指令理解可以委托给更强大、更灵活的 LLM。因此，对相关性标签的需求被去除了。并且论证了 HyDE 在实践中也是有用的。
### 下阶段任务：
- 终于读完论文了，接下来我会下载数据集以及论文的源代码，并尝试进行复现。
（这周因为要忙着校长杯比赛，所以只把论文读完了🙏）