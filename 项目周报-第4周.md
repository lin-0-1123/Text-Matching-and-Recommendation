# 项目周报-第4周

日期：2025-04-25

项目实践题目：文本的匹配与推荐

## 实践内容
### GS 使用体验：
比国内搜索引擎搜到的东西要有用一些，但是不能检测中文内容，所以还是要提升英文水平或者先用翻译软件翻译关键词后搜索。
### 论文阅读——Description-Based Text Similarity：
- 摘要：传统文本嵌入模型在支持抽象语义检索时表现不佳，无法满足实际要求。该论文提出基于“内容抽象描述”的文本相似性概念，强调通过抽象语义描述而非单纯语料统计来检索文本。并通过LLM 模型生成的正负样本对进行训练来提升模型性能。为基于语义的信息检索提供了更优化的解决方案
- 简介：
    - 当前基于密集编码器的语义搜索解决方案存在相似性定义不明确的问题，这些模型通过大量数据学习相似性，但混合了不同类型的相似性，导致在信息检索中效果不佳。作者指出，现有的方法无法有效支持基于抽象描述的文本检索需求。
    - 作者提出利用“描述-文本”对来训练描述编码器和文本编码器，使得描述和他们所描述的文本在嵌入空间中接近。
- 基于描述的相似度：
    - 抽象描述关系：给定的两个文本 T，D ，如果 D 描述了 T 的部分内容，且 D 包含的信息比 T 少同时具体性较低，那么 D 就为 T 的抽象描述。
    - 个人理解：根据定义和给定的例子来看，这些抽象描述与传统的文本摘要不同，摘要会尽可能简单精炼的概括文本的主要内容，并保留关键信息，而抽象描述则不同，虽然也是对文本的概括，但是它会省略一些关键细节，他的描述可能更加抽象，或者说更笼统，或者只描述文本完整事件中的一部分内容。
    - 抽象描述的技术需求：描述与文本几乎没有词汇重叠，导致现有的文本编码器难以处理这种关系，因此作者提出目标：学习文本和描述的嵌入函数 $E_T$ 和 $E_D$，使得 $\text{sim}(E_T(T), E_D(D))$ 与抽象描述关系的相关性更高，即对于满足该关系的 $T$ 和 $D$，其相关性高于所有其他对。
### 下阶段任务：
1. 继续阅读这篇论文
2. 学会LLM 通用 API 的使用