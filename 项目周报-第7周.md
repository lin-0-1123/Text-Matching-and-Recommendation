# 项目周报-第6周

日期：2025-05-18

项目实践题目：文本的匹配与推荐

## 实践内容
### 

### 论文阅读——Precise Zero-Shot Dense Retrieval without Relevance Labels：
- 相关工作这一部分提到了很多在此之前的一些研究，包括密集检索、训练损失、负采样、蒸馏等，这部分引用的内容比较多，下周任务我将会从中挑选一些我感兴趣的内容进行阅读，同时结合 ai 来节省时间
- ​Instruction-Following Language Model (指令跟随语言模型)​​
    - 指令跟随语言模型是一种经过训练的模型，可以根据输入的指令执行特定任务。例如，InstructGPT 就是一种指令跟随语言模型。（instructGPT 相关论文：“Training language models to follow instructions with human feedback”）
    - 这种模型的强大之处在于其灵活性和泛化能力，能够根据不同的指令执行各种任务，而不需要针对每个任务进行单独的训练。
- Generative Retrieval（生成检索）
    - 生成检索使用神经生成模型作为搜索索引，依靠模型生成文档的标识符，并将标识符直接映射到文档中，而不需要使用额外的标识符。
    - 生成检索可以不依赖显式索引，适合复杂查询，同时也依赖生成模型的质量。
- 在阅读了第三部分（方法）后我对HyDE方法有了一定的理解，同时也解答了这个方法的优势为什么是讲检索任务拆解成自然语言理解（NLU）和自然语言生成（NLG）任务：
    - HyDE 方法的过程在之前的周报中提到过，简单来说就是让生成模型生成一个假设文档并将其编码为向量，然后对比相似度来得出与之最接近的真实文档
    - 这个检索方法因为生成文档需要生成模型对人提出的要求进行理解（NLU）并生成一篇与之相符合的假设文档（NLG），而之后的任务就是对生成文档进行相似度匹配，故检索中最终要的任务即是生成模型的自然语言理解与生成能力（好像变成考验生成模型性能了）
### 下阶段任务：
- 阅读Precise Zero-Shot Dense Retrieval without Relevance Labels的第四部分也是最后一部分，当然第三部分也需要在重复阅读一下，他的解决方法我还没有完全理解