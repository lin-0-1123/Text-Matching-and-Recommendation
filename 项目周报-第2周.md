# 项目周报

日期：2025-04-13

项目实践题目：文本的匹配与推荐

## 实践内容

实践内容：
1、继续阅读文本向量化相关论文：
    ·余弦测量相似度：通过测量两个相同维度向量的夹角余弦值来评估他们之间的相似度。其中余弦值的取值为 0-1，值越大表示这两个词越相似，在图中的表现为两个向量的夹角越小。
    ·tf-idf：通过计算每个词在文档中的词频和逆文档频率来评估词的重要性，从而将文档转化为向量表示。其中 idf 可以将文档中出现频率较高的词的权重降低，从而提高了关键词的区分度。
    ·PPMI:跟 tf-idf 相似，不过它更注重词与词之间的相关性而不是词与文档之间的，对于 PMI 对于偏重罕见时间的问题，除了可以使用不同的函数来改变计算，还可以通过拉普拉斯平滑来进行修正（拉普拉斯平滑也可以所谓朴素贝叶斯分类器的修正手段）。
    ·word2vec：通过训练神经网络来学习词向量，从而将文档转化为向量表示。其中 word2vec 可以学习到词语的上下文信息，从而提高了关键词的区分度。

2、登录学校的服务器完成基本的配置任务：
这周主要是完成了登入服务器并在其中安装 Python 及其相应库的任务，并且在其中学会了一些基本的操作，再次熟悉了一下 Linux 基础指令，但是在安装上出现了很多的问题。

✅uv的安装：在 github 的文档中给到了一个独立的安装程序指令。但这条指令貌似不能在学校的服务器中执行，并且出现了 error403 的报错，这可能是因为学校的服务器没有安装🪜的原因导致无法请求 github 中的下载链接，从而导致 uv 无法安装；对此我们可以通过其他指令如“pip install uv”来进行安装（安装时间在 2-3min 左右，稍微慢一点）

❌Python 3.11的安装：使用 uv python install 3.11 进行python 3.11版本的安装时，同样出现了无法通过 github 指定链接下载的问题，估计和 uv 无法安装的问题类似，但是我还没有找到相应的解决方法，如果找到了会修该周报内容并上传新的版本。

✅创建 main.py并运行

✅新增 test.ipynb 并在其中写入代码并运行


补充：目前 GitHub 已屏蔽了国内的 IP 网络，有登录状态的可以正常访问，其他的会返回 403 error。这会导致上述两个问题的出现，以及我的 gitKraken也不能push 我的周报，只能手动提交。（我裂开了）
     修改.python-version文件会直接导致 Python 版本发生变化，如果未安装当前版本会自动进行安装。

下周任务：
1、完成第八周的支线任务（数学公式的书写）
2、完成 slp section6 的阅读