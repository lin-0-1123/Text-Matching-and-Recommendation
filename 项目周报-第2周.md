# 项目周报

日期：2025-04-13

项目实践题目：文本的匹配与推荐

## 实践内容

实践内容：
1、继续阅读文本向量化相关论文
2、登录学校的服务器完成基本的配置任务。

实践收获：
这周主要是完成了登入服务器并在其中安装 Python 及其相应库的任务，并且在其中学会了一些基本的操作，再次熟悉了一下 Linux 基础指令，但是在安装上出现了很多的问题。
uv的安装：在 github 的文档中给到了一个独立的安装程序指令。但这条指令貌似不能在学校的服务器中执行，并且出现了 error403 的报错，这可能是因为学校的服务器没有安装🪜的原因导致无法请求 github 中的下载链接，从而导致 uv 无法安装；对此我们可以通过其他指令如“pip install uv”来进行安装（安装时间在 2-3min 左右，稍微慢一点）
Python 3.11的安装：使用 uv python install 3.11 进行python 3.11版本的安装时，同样出现了无法通过 github 指定链接下载的问题，估计和 uv 无法安装的问题类似，但是我还没有找到相应的解决方法，如果找到了会修该周报内容并上传新的版本。

其他的问题还没有遇到，如果遇到了会及时更新周报的内容

