# 项目周报-第5周

日期：2025-05-11

项目实践题目：文本的匹配与推荐

## 实践内容
### LLM 通用 API 的调用：
- 模型选择：
    - 在免费模型中我挑选的是Qwen3-8B 模型，阿里新推出的大模型，在满血模型表现上和 DeepSeek持平。
- 调用：
    - 使用uv 安装 Python OpenAI 库，进行调用。
- 发现；
    - 由于没有用过Qwen3-8B 模型，在他第一次生成时我以为我调用了 ds-R1 模型，对比发现 Qwen3 也有深度思考的过程，所以模型有被正确的调用，任务完成✅。
### 论文阅读——Precise Zero-Shot Dense Retrieval without Relevance Labels：
- 摘要：
    - 密集检索在没有相关性标签可用时，仍很难创建有效的完全零样本密集检索系统。作者建议通过**假设文档嵌入（HyDE）**进行转换。给方法在各种任务喝语言都优于 Contriever（最先进的无监督密集检索器）
- 简介：
    - 密集检索：
        - 一种语义嵌入相似性检索文档的方法。
        - 负采样、蒸馏、特定任务训练等方法的提出提高了监督密集检索模型的有效性。
    - 零样本密集检索的问题：
        - 即使考虑使用迁移学习，也面对缺乏大型训练集的困境（因为不能总是假设存在这样的大型训练集）
    - 作者的目标：
        - 构建不需要相关性监督、开箱即用且能够跨任务推广的有效零样本密件检索系统。
        - 使用假设文档嵌入（HyDE），将密集检索分解为两个任务：
            - 1、由遵循指令的语言模型执行的生成任务
                - 给生成模型输入令其生成一个与相关文档类似的假设文档。（文档非真实，可能包含事实错误）
            - 2、由（无监督）对比编码器执行文档——文档相似性任务
                - 将生成的假设文档编码为嵌入向量
                - 编码器的密集瓶颈充当有损压缩器，过滤嵌入中的冗余信息
            - 3、使用编码好的向量在语料嵌入库中搜索，返回最相似的真实文档。
    - 实验使用模型： Instruct-GPT（Ouyang et al.，2022）和 Contriever（Izacard et al.，2021）
    - 思考：
        - 假设文档嵌入（HyDE）究竟是监督学习还是无监督学习？
            - 作者说看起来像是未监督的。因为 HyDE 中没有训练任何模型（生成模型和对比编码器都是预先训练好的。转换器编码器保持不变。监督信号仅用于我们的主要语言模型（LLM）的指令学习。），不过按照 HyDE 的原理来看的话，看起来像是一种自监督学习，因为他依靠生成的假设文档来对自己进行训练，根据前文提出零样本密集检索所遇到的问题（缺少大型训练集），HyDE 通过生成假设文档这种方式来生成了大量训练样本，虽然样本可能是错误的。
### 下阶段任务：
- 继续推进论文阅读
- 完成MarkDown和Word其他格式语法的学习
### 其他：关于通义千问
- 这周的论文阅读我使用了通义千问来辅助我进行阅读，在‘发现’->‘阅读助手’中上传所需文档，然后便可以查看，可以完成翻译，绘制脑图等功能，页面为左侧原文右侧翻译，可以点击译文来定位原文位置。缺点是该页面下的智能问答很呆，自己输入的问题迟迟没有反应，需要额外开一个页面进行问答。