# 项目总结报告
针对无相关性标签的精确零点密集检索的问题，主流的一些办法有，BM25 和 Contriever 两种方法，而 HyDE 的提出让零点密集检索问题有了新的突破，下面是这几种方法的介绍。
## BM25（Best Matching 25）
BM25 是一种基于概率模型的信息检索算法，通过计算文档与查询之间的相关性来对文档进行排序，它依赖于关键词匹配，适用于零训练数据的场景。我们给定一个查询（Query）和一个文档（Document），它们之间的相关性得分（Relevance Score），也就是 BM25 分数，其核心公式如下：
### **BM25 公式**

$$
\text{BM25}(D, Q) = \sum_{i=1}^{n} \log \left( \frac{N - n(q_i) + 0.5}{n(q_i) + 0.5} \right) \cdot \frac{f(q_i, D) \cdot (k_1 + 1)}{f(q_i, D) + k_1 \cdot \left(1 - b + b \cdot \frac{|D|}{\text{avgdl}}\right)}
$$
### **符号说明：**

- $ D $: 被评分的文档  
- $ Q $: 用户输入的查询词集合，$ Q = \{q_1, q_2, ..., q_n\} $  
- $ f(q_i, D) $: 查询词 $ q_i $ 在文档 $ D $ 中出现的次数（Term Frequency, TF）  
- $ |D| $: 文档 $ D $ 的长度（总词数）  
- $ \text{avgdl} $: 所有文档的平均长度  
- $ N $: 文档总数  
- $ n(q_i) $: 包含查询词 $ q_i $ 的文档数量  
- $ k_1, b $: 可调参数，通常设置为 $ k_1 \in [1.2, 2.0] $，$ b = 0.75 $
## Contriever（Contrastive Retriever）
Contriever 是一种基于对比学习、无监督训练的稠密信息检索模型，这是基于传统稠密检索器在有监督学习的情况下表现良好，但零样本转移能力弱而进一步提出的模型，其核心是基于对比学习进行的预训练，这种方法可以在没有人工标注的情况下训练出强大的文档检索模型。
### 模型架构（做简单叙述，难的我也讲不清楚）
Contriever 是一种双编码器结构的稠密检索模型，由以下关键部分组成：
- 使用 Transformer（BERT base uncased） 作为主干编码器。
- 共享参数：query 和 document 使用同一个编码器 fθ（不像 DPR 那样用两个编码器）
- 编码方式：对句子的最后一层 hidden states 取平均，作为其稠密向量表示。
## HyDE（Hypothetical Document Embeddings）
HyDE是一种基于嵌入（embedding）的检索增强方法，它通过生成“假设性文档”来提升信息检索系统的性能，尤其是在与稠密向量检索（如DPR、Contriever等）结合使用时效果显著。
### 模型架构
HyDE 的模型架构比较简单，主要由两部分组成：
- 大型预训练语言模型（LLM）：如 GPT-3。
- 无监督对比编码器：如 Contriever。
### 模型原理
HyDE 的工作原理主要分为以下几步：
- 首先用户发送一个查询（Query），例如：“量子计算的基本原理是什么？”。
- LLM 模型在接收到查询后，会根据查询内容生成一个假设文档，这个文档是模型自己撰写的（因此可能存在错误）。
- 接着使用稠密检索模型（如 SBERT、Contriever、DPR）将假设文档编码为一个稠密向量（也就是嵌入）。
- 将嵌入与文档库中的所有文档的嵌入进行相似度比较（如使用余弦相似度），找出相似度最接近的**真实文档**。
### HyDE的优势
在该问题上，HyDE 确实比上述的方法都要好，在原理上利用 LLM 生成假设文档，再将假设文档编码为嵌入后在进行比较找出最相似的文档，这使得该模型更加依赖 LLM 的自然语言理解（NLU）和自然语言生成（NLG）两项性能，而 Contriever 等模型在该模型中充当的则是无监督对比编码器的功能，因此检索的结果好坏会更加依赖 LLM 的性能，无监督对比编码器也会一定程度上对检索结果造成影响，但是影响程度并不大。而且相比于稠密检索模型在使用前需要大量数据集进行训练，HyDE 因为会生成假设文档嵌入，相当于每次查询都能给自己提供数据集，因此更加适合零样本密集检索问题，在无相关性标签的前提下 HyDE 有比BM25 和 Contriever 更好的表现。

